[pytest]

testpaths = tests

python_files = test_*.py

addopts = -v

pythonpath = .

markers =
    integration: tests that require external services such as Docker Compose, PostgreSQL, or other running infrastructure
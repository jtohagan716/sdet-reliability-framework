[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sdet-reliability-framework"
version = "0.1.0"
description = "Reliability-focused SDET automation framework"
authors = [
    {name = "James O'Hagan"}
]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["framework"]